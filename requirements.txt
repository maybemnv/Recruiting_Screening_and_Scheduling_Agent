# The API runtime uses the Python standard library. These pinned HTTP stack
# versions keep environments that exercise optional provider adapters warning-free.
requests==2.32.5
urllib3==2.5.0
charset-normalizer==3.4.3
chardet==5.2.0
boto3==1.43.96
